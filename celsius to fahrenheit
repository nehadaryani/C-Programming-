#include<stdio.h> //celsius to fahrenheit
float convert(float C){
    float far=C*(1.8)+32;
    return far;
}
int main(){
    float C;
    printf("Enter Temperature in Celsius: ");
    scanf("%f",&C);
    float far=convert(C);
    printf("The Temperature in Fahrenheit is: %f",far);
    return 0;
}
