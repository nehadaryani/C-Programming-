#include<stdio.h>
int main(){
    int i,n;
    printf("Enter n: ");
    scanf("%d",&n);
    for (int i = 0; i<=n; i++)
    {
        if(i==n-4){
            continue;
        }
        printf("%d ",i);
    }
    
    return 0;
}
