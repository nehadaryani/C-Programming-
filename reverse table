#include<stdio.h>
int main(){
    int n;
    printf("Enter n: ");
    scanf("%d",&n);
    printf("The Table of n is as follows: \n");
    for (int i = 10; i>=1; i--)
    {
        printf("%d * %d = %d \n",n,i,n*i);
    }
    
    return 0;

}
