#include<stdio.h>
#include<math.h>
int main(){
    int n;
    printf("Enter n: ");
    scanf("%d",&n);
    printf("The Square of n is : %f",pow(n,2)); //pow function always takes only float value
    printf("\nThe cube of n is: %f",pow(n,3));
    return 0;
}
